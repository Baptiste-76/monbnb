<nav class="navbar navbar-expand-lg navbar-dark bg-primary fixed-top">
    <a class="navbar-brand" href="{{ path('homepage') }}">Mon BnB !</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    
    <div class="collapse navbar-collapse" id="navbarColor02">
        {# mr auto permet de coller l'élément à gauche #}
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="{{ path('homepage') }}">
                    <i class="fas fa-home mr-2"></i>
                    Accueil
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('ads_index') }}">
                    <i class="fas fa-bullhorn mr-2"></i>
                    Annonces
                </a>
            </li>
        </ul>
        {# ml auto permet de coller l'élément à droite #}
        <ul class="navbar-nav ml-auto">
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        <img src="{{ app.user.picture }}" class="avatar avatar-mini mr-2" alt="Avatar de {{ app.user.fullname }}">
                        {{ app.user.fullname }}
                    </a>

                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('ads_create') }}" class="dropdown-item">
                            <i class="fas fa-plus mr-2"></i>
                            Créer une annonce
                        </a>

                        <div class="dropdown-divider" style="border-color: white;"></div>

                        <a href="{{ path('account_index') }}" class="dropdown-item">
                            <i class="fas fa-user mr-2"></i>
                            Mon compte
                        </a>
                        <a href="{{ path('account_bookings') }}" class="dropdown-item">
                            <i class="fas fa-book mr-2"></i>
                            Mes réservations
                        </a>
                        <a href="{{ path('account_profile') }}" class="dropdown-item">
                            <i class="fas fa-user-edit mr-2"></i>
                            Modifier mon profil
                        </a>
                        
                        <div class="dropdown-divider" style="border-color: white;"></div>

                        <a href="{{ path('account_logout') }}" class="dropdown-item">
                            <i class="fas fa-power-off mr-2"></i>
                            Se déconnecter
                        </a>
                    </div>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('account_register') }}" class="nav-link">
                        <i class="fas fa-address-card mr-2"></i>
                        Inscription
                    </a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('account_login') }}" class="nav-link">
                        <i class="fas fa-sign-in-alt mr-2"></i>
                        Connexion
                    </a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}